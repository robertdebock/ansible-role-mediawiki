---
dependency:
  name: galaxy
  options:
    role-file: requirements.yml
driver:
  name: docker
lint:
  name: yamllint
platforms:
  - name: mediawiki-debian-unstable
    image: debian:unstable
provisioner:
  name: ansible
  lint:
    name: ansible-lint
  inventory:
    group_vars:
      all:
        httpd_ignore_docker: no
        mysql_ignore_docker: no
scenario:
  name: debian-unstable
verifier:
  name: goss
  lint:
    name: yamllint
